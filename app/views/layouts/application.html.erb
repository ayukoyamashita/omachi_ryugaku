<!DOCTYPE html>
<html lang="ja">
<head>
	<title>しずおか おまち留学</title>
	<meta name="viewport" content="width=device-width,initial-scale=1">
	<meta name="robots" content="noindex" />
	<%= csrf_meta_tags %>
	<%= csp_meta_tag %>

	<%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
	<%= yield(:add_css) %>
</head>

<body>
<div class="container">
	<%= yield %>
</div>

<%= yield(:add_js) %>
</body>
</html>
